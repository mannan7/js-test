function add(x, y) {
    return x + y
  }
  function subtract(x, y) {
    return x - y
  }
  function check(x, y) {
    return x === y
  }
  module.exports = { add: add, subtract: subtract, check: check }